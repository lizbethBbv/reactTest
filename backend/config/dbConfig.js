const dbConfig = {
        host: 'localhost',
        user: 'atrater',
        password: 'password',
        database: 'atrato',
        waitForConnections: true,
        connectionLimit: 10,
        queueLimit: 0,
}

module.exports = dbConfig;